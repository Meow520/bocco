// スタート画面（ルート）
import React from "react";
import { HomeMain } from "@/components/features/home/HomeMain";

const Home = () => {
  return (
    <main className="min-h-screen flex justify-center items-center">
      <HomeMain />
    </main>
  );
};

export default Home;
